#include <bits/stdc++.h>
#include <conio.h>

using namespace std;

int main()
{
    char ch;
    cout << "Enter the character : ";
    cin >> ch;
    int ascii = ch;
    cout << "\nThe ASCII Value of " << ch << " is : " << ascii;
    getch();
    return 0;
}
